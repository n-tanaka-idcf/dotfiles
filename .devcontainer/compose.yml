---
services:
  ubuntu:
    build:
      context: ubuntu/
      dockerfile: Dockerfile
      args:
        - UID
        - GID
    tty: true
    stdin_open: true
    restart: always
    volumes:
      - ../:/workspace
